import { PageSkeleton } from "@/components/feedback/Skeleton";

/**
 * App Router loading UI — shown while a page segment is streaming.
 */
export default function RootLoading() {
  return <PageSkeleton />;
}
